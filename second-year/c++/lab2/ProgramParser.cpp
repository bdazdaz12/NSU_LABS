#include "ProgramParser.h"

void ProgramParser::parseProgram(const string &file, const string &inputFile, const string &outputFile) {
    ifstream workflowFile(file);

}

//bool ProgramParser::isValid(){}


////
//map<int , shared_ptr<IWorker>> blocks;
//blocks[1] = make_shared<DumpBlock>();
//list<int> queue = {1, 2};
//ProgramExecutor().executeWorkflow({queue, blocks});
////